# Alembic governs the app schema only. The morpholog schema is the
# substrate's own, provisioned by `morpholog init`, and is never
# touched by these migrations.
[alembic]
script_location = alembic
sqlalchemy.url = postgresql+psycopg://glasshouse:glasshouse@localhost:5432/glasshouse
